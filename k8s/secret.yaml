# IMPORTANT: Replace placeholder values before applying!
# Encode values with: echo -n "your-value" | base64
#
# Apply with: kubectl apply -f k8s/secret.yaml
# Or use a secrets manager (Vault, Sealed Secrets, External Secrets Operator)

apiVersion: v1
kind: Secret
metadata:
  name: n8n-secret
  namespace: n8n
type: Opaque
data:
  # echo -n "your-32-char-encryption-key" | base64
  N8N_ENCRYPTION_KEY: eW91ci0zMi1jaGFyLWVuY3J5cHRpb24ta2V5
  # echo -n "https://your-domain.com/" | base64
  WEBHOOK_URL: aHR0cHM6Ly95b3VyLWRvbWFpbi5jb20v
